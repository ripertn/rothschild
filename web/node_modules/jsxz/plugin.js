var ast_transform = require('./index.js')

module.exports.default = function() {
  return {
    inherits: require("babel-plugin-syntax-jsx"),
    pre(state) {
      this.cache = new Map()
    },
    visitor: {
      StringLiteral(path) {
        this.cache.set(path.node.value, 1)
      }
    },
    post(state) {
      console.log(this.cache)
    }
  }
}
